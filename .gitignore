# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# 환경 설정 (민감 정보)
.env

# 로컬 DB (GitHub Actions에서는 캐시로 관리)
data/*.db
data/*.db-journal
data/*.db-shm
data/*.db-wal

# 로그 / 세션 중간 파일
logs/

# 테스트 캐시
.pytest_cache/

# Claude Code 로컬 설정
.claude/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# docs/data/ 의 JSON 파일은 Actions이 생성 — 초기 placeholder는 커밋 포함
# (아래 주석 처리로 JSON 추적 허용)
# docs/data/*.json

# Google 서비스 계정 키 파일 (절대 커밋 금지)
researchsearch-*.json
*-service-account*.json
*serviceaccount*.json
service_account*.json
